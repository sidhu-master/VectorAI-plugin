{
  "name": "@vectorai/drawing-spatial",
  "version": "0.1.0-alpha.0",
  "description": "Host-independent bounded spatial queries for VectorAI Drawing documents",
  "type": "module",
  "license": "Apache-2.0",
  "sideEffects": false,
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run src"
  },
  "dependencies": {
    "@vectorai/drawing-core": "workspace:*",
    "@vectorai/drawing-hatch": "workspace:*"
  }
}
