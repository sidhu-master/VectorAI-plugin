# 自适应图元分段与拓扑部件选择设计

## 1. 目标

本次迁移解决两个互相影响的根因：

1. 清洁线稿中的一条骨架链只能提升为一个解析图元，导致手臂斜线与身体竖线被保留成共享 Polyline。
2. 语义编辑使用区域求交授权图元，导致与目标空间重叠、但语义上不属于目标的身体和头部被一起修改。

迁移后的系统必须满足：

- 明显由多个解析图元组成的骨架链可以一对多提升。
- 分段阈值不得依赖固定像素值，缩放图纸后决策保持一致。
- 平滑轮廓、轻微锯齿和有机曲线不得被无意义地拆碎。
- 底层图元可以适度分段，但通过 CompoundPath 保持同一逻辑整体。
- 模型通过语义种子和接口节点表达目标，后端沿显式拓扑提取目标子图。
- 搜索区域只负责裁剪视觉上下文，不再授予几何写权限。

## 2. 已验证的实验基线

验证使用 `test2.png` 的真实矢量化输出，不修改生产代码。

- 图像尺寸：3058 × 4103。
- 全图中位线宽：10.624575 px。
- 骨架链数量：68。
- 问题链：`chain_8606291be0eebf50875a`。
- 问题链采样点：517。
- 问题链长度：约 940.88 px。
- 原始整体直线拟合 P95 误差：约 42.52 px。
- 自适应分段点：第 59 个采样点，坐标约 `(727.76, 2858.11)`。
- 分段后两条直线的局部 P95 误差：约 1.65 px 与 0 px。

参数扰动覆盖：

- 拐角下限：24°、28°、32°、36°。
- 多尺度稳定比：0.72、0.78、0.84。
- 拟合收益门槛：1.0、1.5、2.0 个归一化容差单位。

全部 36 组组合都在同一位置拆分问题链。图形缩放到 0.25、0.5、1、2、4 倍时，分段索引保持不变。

对照结果：

- 平滑头发曲线：0 个分段点。
- 轻微锯齿长线：0 个分段点。
- 六段明显折线组成的头发轮廓：5 个内部分段点，并由一个 CompoundPath 保持整体。
- `test2` 全部 68 条链：只有 5 条需要新增内部边界，每条 1 个，没有全图碎片化。

## 3. 自适应分段算法

### 3.1 尺度变量

算法使用以下尺度：

- `D`：整张图纸包围盒对角线。
- `W`：整张图纸的稳健中位线宽。
- `L`：当前骨架链的弧长。

所有长度门槛都由 `D`、`W`、`L` 的比例产生，不设置固定像素阈值。角度、误差比和收益值是无量纲常数，可以固定并通过回归集校准。

初始验证采用：

```text
fitTolerance = max(0.50 × W, 0.00030 × D)
nearWindow   = max(2.00 × W, 0.00250 × D)
farWindow    = max(4.00 × W, 0.00500 × D)
minimumSpan  = max(4.00 × W, 0.00500 × D, 0.03 × L)
```

这些比例是回归配置，不散落在拟合代码中。生产实现必须输出实际使用的尺度和得分，便于审计和重新标定。

### 3.2 候选拐角生成

对每个骨架链按弧长采样，在 `nearWindow` 和 `farWindow` 两个尺度分别使用 PCA 估计进入与离开方向。

候选拐角同时满足：

1. 近尺度和远尺度的方向变化均超过角度门槛。
2. 两个尺度的角度比达到稳定门槛，排除平滑圆弧中随窗口扩大而增长的曲率。
3. 候选点到链两端及相邻候选点的距离不小于 `minimumSpan`。
4. 同一个线宽范围内的连续候选合并成一个“拐角区”，只保留得分最高的候选。

### 3.3 候选验证与模型选择

候选点不会因为角度足够大而直接成为分段点。系统在候选附近搜索最优切点，并分别拟合左右局部窗口。

```text
splitGain = (
  combinedRobustError
  - weightedChildRobustError
) / fitTolerance

acceptScore = splitGain - splitPenalty
```

初始 `splitPenalty` 为 1.5 个归一化容差单位；实验已覆盖 1.0～2.0，问题链在整个区间内决策不变。每新增一个内部边界都单独支付一次 `splitPenalty`，因此增加更多图元必须持续带来新的拟合收益。

只有在以下条件同时满足时才拆分：

- 左右子段均能稳定拟合为 Line、Arc 或其他已支持的解析图元。
- `acceptScore > 0`。
- 新子段均达到 `minimumSpan`。

`splitPenalty` 与 `minimumSpan` 共同构成复杂度惩罚，避免为了降低少量误差生成大量短图元。

### 3.4 解析图元优先级

处理顺序为：

1. 整链可稳定拟合成 Circle、Ellipse、Arc 或 Line 时，保留整链解析图元，不进入分段。
2. 整链无法稳定拟合时，运行多尺度拐角检测和局部模型选择。
3. 可解释的子段提升为 Line、Arc、Circle、Ellipse。
4. 无法稳定解释的剩余部分保留为 Polyline；后续支持 Spline 时可以提升为 Spline。

闭合链必须使用循环索引处理首尾连接，不能把数组首尾当成天然断点。

## 4. CompoundPath 逻辑整体

一条来源骨架链可以产生多个 Drawing IR 图元，但同时生成一个不可渲染的逻辑容器：

```ts
interface CompoundPath {
  id: string;
  memberIds: GeometryId[];
  sourceChainId: string;
  closed: boolean;
  ordered: boolean;
  junctions: PathJunction[];
}

interface PathJunction {
  atMemberIndex: number;
  point: Vec2;
  kind: 'continuous' | 'corner' | 'topology';
}
```

约束：

- `memberIds` 按原始路径顺序排列。
- CompoundPath 本身不复制几何数据，也不改变渲染结果。
- 默认选择、移动、删除和模型摘要把 CompoundPath 当作一个逻辑对象。
- 精确编辑和拓扑寻路可以访问内部成员与接口节点。
- 每个成员保留相同的 `sourceChainId` 和原始采样范围，支持审计、回归和重新拟合。

CompoundPath 是路径身份，不是 CAD 图块或图层，不引入实例化和样式继承语义。

## 5. 全局几何拓扑图

Drawing IR 每个 revision 派生一个 `GeometryTopologyGraph`。图包含：

- 解析图元端点。
- Polyline/Spline 的稳定顶点与参数化虚拟端口。
- 经导入或审计确认的真实连接点。
- CompoundPath 内部的有序连接关系。
- Drawing IR 已存在的显式拓扑关系。

视觉相交不得自动成为拓扑连接。只有显式关系、共享稳定端点或经过容差验证并记录证据的连接，才允许建立图边。

拓扑图绑定 revision。几何事务提交后旧图失效并增量重建受影响区域。

## 6. 语义节点与部件子图提取

模型不再返回“区域内所有可修改图元”，而是调用以下类型的工具契约：

```ts
interface SemanticPartQuery {
  targetSeeds: GeometryAnchor[];
  boundaryPorts: GeometryAnchor[];
  requiredWaypoints?: GeometryAnchor[];
  protectedSeeds?: GeometryAnchor[];
}

interface GeometryAnchor {
  geometryId: GeometryId;
  parameter?: number;
  point?: Vec2;
  role: 'seed' | 'boundary' | 'required' | 'protected';
}
```

模型负责语义判断：哪里是手掌、手臂与身体的接口在哪里、哪些分支属于身体。后端负责确定性几何：

1. 将模型锚点吸附到 revision 对应的真实图元或参数化虚拟端口。
2. 从目标种子出发，枚举被边界端口截断的候选路径、环和连通子图。
3. 使用切线连续性、视觉掩码支持、必经点、保护点和显式拓扑关系评分。
4. 唯一候选直接授权；多个近似候选返回 Top-K 供模型结合局部渲染选择。
5. 没有足够证据时返回低置信度，不扩大搜索区域并授权全部交集。

搜索区域只用于裁剪截图、降低上下文和限制候选枚举范围，不参与最终写权限计算。

## 7. test2 预期结果

迁移后，问题共享链产生：

- 一条斜向手臂 Line。
- 一条竖直身体 Line。
- 一个位于肩部的拓扑接口节点。
- 一个包含上述来源关系的 CompoundPath。

用户请求“把图形的右手抬起来打招呼”时：

1. 模型选择手掌/手臂种子和肩部上下接口。
2. 后端沿拓扑提取手臂上下边界与手掌轮廓。
3. 身体竖线在肩部接口处被截断，不进入授权子图。
4. 修改后验证手臂闭合、仍与身体连接、非目标图元哈希不变。

## 8. 回归与可审计性

正式实现必须包含：

- 问题链拆成两条 Line 的确定性测试。
- 同一链多倍率缩放后分段索引一致的测试。
- 平滑头发曲线不拆分的测试。
- 明显折线头发内部可分段、逻辑上只有一个 CompoundPath 的测试。
- 轻微锯齿线不拆分的测试。
- 闭合曲线首尾连续的测试。
- 视觉相交但无拓扑关系的图元不会进入部件子图的测试。
- test2 手臂种子与肩部接口只提取手臂、不提取身体竖线的集成测试。

每次分段记录：算法版本、`D/W/L`、候选点、角度、多尺度稳定比、父子拟合误差、复杂度惩罚、接受或拒绝原因。每次部件选择记录：revision、模型锚点、吸附结果、候选子图、评分和最终授权集合。

## 9. 非目标

- 本次不引入图层、图块、填充或 3D 数据。
- 本次不把所有 Polyline 强制转换为解析图元。
- 本次不让视觉区域直接拥有写权限。
- 本次不以 test2 的绝对像素或特定坐标编写特例。
